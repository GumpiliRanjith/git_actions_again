name: experi
on: push

jobs:
  ranjith:
    runs-on: ubuntu-latest

    steps:
      - name: Print Name
        run: echo "Hello ${{ secrets.NAME}}"

  kumer:
    needs: ranjith
    runs-on: ubuntu-latest

    steps:
      - name: kumer
        run: echo "HELLO $name"
