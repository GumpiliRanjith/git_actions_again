name: experi
on: push

jobs:
  ranjith:
    runs-on: ubuntu-latest

    steps:
      - name: took at repo
        run: echo "hello"

  kumer:
    needs: ranjith
    runs-on: ubuntu-latest

    steps:
      - name: kumer
        run: echo "HELLO"

  ravi:
    needs: [ranjith,kumer]
    runs-on: ubuntu-latest
    steps:
      - name: welcome flow
        run: echo "MASTER"
    
  master:
    needs: [ranjith,kumer,ravi]
    runs-on: ubuntu-latest
    steps:
      - name: welcome high
        run: echo "high"


