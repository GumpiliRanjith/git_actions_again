name: exp1

on: push

jobs:
  ranjith:
    runs-on: ubuntu-latest

    steps:
      - name: lookup repo
        uses: actions/checkout@v4

      - name: setupNode
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: install dependencie
        run: npm ci

        