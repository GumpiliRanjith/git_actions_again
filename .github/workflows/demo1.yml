name: exp1

on: push

jobs:
  space:
    runs-on: ubuntu-latest

    steps:
      - name: profile
        run: echo "G Pulla Reddy"

  ranjith:
    runs-on: ubuntu-latest

    steps:
      - name: print
        run: echo "hello world"
  kumer:
    needs: ranjith
    runs-on: ubuntu-latest

    steps:
      - name: again
        run: echo "needed from ranjith .... .."
    
  output:
    needs: kumer
    runs-on: ubuntu-latest

    steps:
      - name: autonomous
        run: |
              echo "Deploying .. ..."
              echo "Successfully deployed..."

    

    